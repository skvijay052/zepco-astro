---
interface Props {
  variant?: 'header' | 'footer';
}
const { variant = 'header' } = Astro.props;
---
<span class:list={['brand', variant]}>
  <img
    class="brand-logo"
    src="/images/zepco-logo.webp"
    alt="Zepco Technologies"
    width="1342"
    height="520"
    loading={variant === 'header' ? 'eager' : 'lazy'}
    decoding="async"
  />
</span>
<style>
  .brand { display: inline-flex; flex-shrink: 0; align-items: center; padding: 4px 8px; border-radius: 2px; background: #fff; vertical-align: middle; }
  .brand-logo { display: block; width: 124px; height: auto; }
  .footer .brand-logo { width: 184px; }
</style>
